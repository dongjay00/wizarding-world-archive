# scaffold

You are executing `AGENTS.md §4` step `scaffold`.

## Read

- `AGENTS.md`
- `docs/runtime/session.md`
- `docs/state/architecture/Architecture.md`
- `docs/state/architecture/SDD/*`
- `docs/runtime/tasks.md`

## Write

- Code skeletons only
- `docs/runtime/session.md` verdict

## Work

Create the minimum directories, files, types, signatures, and empty modules
needed by the SDD. Leave UI details and business logic to later steps.

## Done

The skeleton compiles with `npx tsc --noEmit`. If it cannot, record the reason
and rewind to `architect`.
